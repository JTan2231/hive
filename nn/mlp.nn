let head = normal(1, 64)
let hidden = normal(64, 32)
let out = normal(32, 1)

let model_input = input(t, 1, 1)
let label = input(sine_value, 1, 1)

let x = matmul(model_input, head)
x = matmul(x, hidden)
x = matmul(x, out)

let mse = subtract(x, label)
mse = pow(mse, 2)

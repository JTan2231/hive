var head = normal(32, 1, 64)
var hidden = normal(32, 64, 32)
var out = normal(32, 32, 1)

let model_input = input(t, 32, 1, 1)
let label = input(sine_value, 32, 1, 1)

let x = matmul(model_input, head)
x = relu(x)
x = matmul(x, hidden)
x = relu(x)
x = matmul(x, out)

let mse = subtract(x, label)

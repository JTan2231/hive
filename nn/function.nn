function my_function(a, b, c)
    let d = matmul(c, matmul(a, b))
    d = matmul(d, matmul(d, d))
end

let a = normal(3, 5)
let b = normal(5, 3)
let c = normal(3, 3)
let d = normal(3, 3)
let e = normal(3, 3)
let f = normal(3, 3)

let g = my_function(a, b, c)
let h = my_function(d, e, f)
let i = my_function(g, h, f)
